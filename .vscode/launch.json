{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug after build",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build",
      "stopAtEntry": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb.exe"
    }
  ]
}

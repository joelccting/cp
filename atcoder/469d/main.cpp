#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N, M;
    if (!(cin >> N >> M))
        return 0;
    
    while (M--)
    {
        int A, B;
        cin >> A >> B;
    }
    return 0;
}